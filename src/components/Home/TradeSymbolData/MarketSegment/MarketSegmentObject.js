const tickerHeader = {
  color: "#808090",
  textAlign: "center"
};

const tickerValue = {
  marginTop: "4px"
};

const marketSegmentBox = {
  height: "60px"
};

const flexObject = {
  display: "flex",
  width: "inherit",
  height: "100%"
};

const loaderObject = {
  width: "5%",
  margin: "25px auto 0",
  textAlign: "center"
};

export const Mark = "Mark Price (USDT)";
export const Index = "Index";
export const twentyfourHr_change = "24h Change";
export const Open_interest = "Open interest (USDT)";
export const _24hrVolume = "24h volume (USDT)";
export const Funding_Countdown = "Funding Fee / Countdown";
export const _24h_High = "24h";
export const _24h_Low = "24h Low";
export const _24HighLow = "24h High / 24h Low";

export { tickerValue, tickerHeader, marketSegmentBox, flexObject, loaderObject };
